[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn app.server:app --host 0.0.0.0 --port ${PORT:-8002}"
healthcheckPath = "/api/config"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
